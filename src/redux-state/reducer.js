const init = [{key:Math.random(),content:"Buy some chocos"},{key:Math.random(),content:"Buy some fruits"}]

const reducerFunction = (state= init, action)=>{
    return state
}

export default reducerFunction